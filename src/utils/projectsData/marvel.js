import { marvelUrl as url } from "../urls";
import image from "../images/marvel.jpg";

const title = "Marvel Heroes Search";
const description = "Search your favourite heroes and comics here!";
const dataMarvel = {
  image,
  url,
  description,
  title,
};

export default dataMarvel;
